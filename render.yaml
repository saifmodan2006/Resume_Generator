# Render Configuration for Puppeteer

# This configuration sets Puppeteer to use the system installed Chrome instead of downloading Chromium.

# Use the full path to the installed chrome
puppeteer:
  executablePath: '/usr/bin/google-chrome-stable'
  skipChromiumDownload: true
