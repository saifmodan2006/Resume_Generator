{
  "name": "resume-builder-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npx puppeteer browsers install chrome || true",
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "npm run postinstall && node scripts/build-check.js"
  },
  "dependencies": {
    "dotenv": "latest",
    "express": "latest",
    "openai": "latest",
    "puppeteer": "latest",
    "zod": "latest"
  }
}